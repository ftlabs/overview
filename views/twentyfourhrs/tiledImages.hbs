<!DOCTYPE html>
<html>
	{{#> head title='24hrs v2 - Image Display' subtitle='Displaying the images of the last 24 hours of published articles' }}
		<style type="text/css">
			html, body {
				margin: 0;
				padding: 0;
				font-size: 0;
				overflow: hidden;
			}
			main {
			}
			a {
		        font-size: 0;
			}
			img {
			}
			header {
				display: none;
			}
		</style>
	{{/head}}
	<body>
		{{> header title='24hrs v2 - Image Display' subtitle='Displaying the images of the last 24 hours of published articles'}}
		<main>
			{{#each results}}
			{{#if images.0.url}}
			<a href="{{ location.uri }}" title="{{ title.title }}" target="_blank">
				<img src="{{imgPath images.0.url}}&width=500&height=281" alt="title="{{ title.title }}""></img>
			</a>
			{{/if}}
			{{/each}}
		</main>
		<script type="text/javascript" src="/static/js/imgGrid/imageGrid.js"></script>
	</body>
</html>