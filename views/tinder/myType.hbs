<!DOCTYPE html>
<html>
	{{#> head title='Tinder - My Type' subtitle='Reading list of all your FT needs' }}
		<style type="text/css">
			html, body {
				text-align: center;
			}
		</style>
	{{/head}}
	<body>
		<main>
		<h1>Reading List</h1>
			{{#each results}}
			<a href="{{ link }}" title="{{ title }}" target="_blank">
				<img src="{{imgPath url}}&width=500&height=281" alt="title="{{ title }}""></img>
			</a>
			{{/each}}
		</main>
	</body>
</html>